package com.assetvault;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Entry point for the AssetVault Spring Boot application.
 */
@SpringBootApplication
public class AssetVaultApplication {
	/**
	 * Starts the AssetVault Spring Boot application.
	 *
	 * @param args the application startup arguments
	 */
	public static void main(String[] args) {
		SpringApplication.run(AssetVaultApplication.class, args);
	}
}
